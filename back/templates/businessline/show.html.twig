{% extends 'base.html.twig' %}

{% block title %}Businessline{% endblock %}

{% block body %}
    <h1>Businessline</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ businessline.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ businessline.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('businessline_index') }}">back to list</a>

    <a href="{{ path('businessline_edit', {'id': businessline.id}) }}">edit</a>

    {{ include('businessline/_delete_form.html.twig') }}
{% endblock %}
