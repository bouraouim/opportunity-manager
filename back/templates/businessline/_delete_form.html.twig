<form method="post" action="{{ path('businessline_delete', {'id': businessline.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ businessline.id) }}">
    <button class="btn">Delete</button>
</form>
