<form method="post" action="{{ path('userr_delete', {'id': userr.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ userr.id) }}">
    <button class="btn">Delete</button>
</form>
