<form method="post" action="{{ path('businessunit_delete', {'id': businessunit.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ businessunit.id) }}">
    <button class="btn">Delete</button>
</form>
